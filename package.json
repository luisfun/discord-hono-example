{
  "name": "discord-hono-example",
  "version": "0.0.0",
  "author": "Luis (https://github.com/luisfun)",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "fix": "biome check --apply . && tsc --noEmit",
    "fix:unsafe": "biome check --apply-unsafe . && tsc --noEmit",
    "register": "tsc && node dist/register.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "log": "wrangler tail"
  },
  "devDependencies": {
    "@biomejs/biome": "1.7.1",
    "@cloudflare/workers-types": "^4.20240423.0",
    "discord-api-types": "^0.37.83",
    "dotenv": "^16.4.5",
    "typescript": "^5.4.5",
    "wrangler": "^3.52.0"
  },
  "dependencies": {
    "discord-hono": "^0.6.1"
  }
}
